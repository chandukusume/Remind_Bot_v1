# Dockerfile.actions
FROM rasa/sdk:latest

# Copy only the actions folder
WORKDIR /app
COPY actions/ /app/actions

# Expose the port the action server runs on
EXPOSE 5055

# Command to run when the container starts
CMD ["run", "actions"]